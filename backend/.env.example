DEEPSEEK_API_KEY=sk-your-key-here
DEEPSEEK_MODEL=deepseek-v4-pro
DEEPSEEK_BASE_URL=https://api.deepseek.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://8.217.224.101

# Optional. When set, the /api/custom and /api/ai endpoints require an
# `X-API-Key: <this value>` header. Leave empty to disable auth (local/demo).
API_KEY=
